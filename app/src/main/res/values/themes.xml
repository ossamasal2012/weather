<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. Compose draws everything; this XML theme only
         configures window-level behavior that must exist before the first
         Compose frame (edge-to-edge, transparent system bars). -->
    <style name="Theme.Weather" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="27">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="28">shortEdges</item>
        <item name="android:forceDarkAllowed" tools:targetApi="29">false</item>
    </style>

    <!-- Launch theme: MUST inherit from Theme.SplashScreen for the
         androidx.core:core-splashscreen compat library to work correctly on
         API 26–30 (API 31+ uses the platform SplashScreen directly under the
         hood). postSplashScreenTheme is where the library automatically
         switches once installSplashScreen() hands off in MainActivity. -->
    <style name="Theme.Weather.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Weather</item>
    </style>

</resources>
